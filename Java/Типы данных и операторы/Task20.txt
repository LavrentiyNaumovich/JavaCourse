После строки a1 = a2;, массив, на который указывает a1, станет {6, 7, 8, 9, 10}, 
а массив, на который указывает a2, останется неизменным, но теперь обе переменные будут ссылаться на один и тот же массив.